-- Root module for the `ODE` library (Lake build target).
-- Re-exports the Euler-convergence development so `lake build` has a root to
-- compile; mirrors the Mathlib.lean / <Lib>.lean convention.
import ODE.EulerConvergence
